This directory contains all our utilities concerning deployment, on container or virtual machines.

## giraph
Contains the Dockerfile, scripts and jar to deploy our Giraph microservice in a docker container
